<Window x:Class="TextEditorApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextEditorApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <!-- Описание стиля для кнопок выбора режима -->
        <Style x:Key="EditMode-ToggleButton" TargetType="ToggleButton">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="20, 0, 0, 0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- Состояние при наведении курсора -->
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <!-- Состояние при нажатии -->
                                <Setter TargetName="Bd" Property="Background" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                BorderBrush="DimGray" 
                BorderThickness="2, 0, 2, 2" 
                CornerRadius="5"
                Padding="0, 5" 
                Margin="20, 0, 20, 0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <ToggleButton Content="Ж" 
                              FontWeight="Bold"
                              Style="{StaticResource EditMode-ToggleButton}"/>
                <ToggleButton Content="К"
                              FontStyle="Italic"
                              Style="{StaticResource EditMode-ToggleButton}"/>
                <ToggleButton Style="{StaticResource EditMode-ToggleButton}">
                    <TextBlock Text="Ч" TextDecorations="Underline"/>
                </ToggleButton>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="200, 10, 200, 10">
            <RichTextBox />
        </Border>
    </Grid>
</Window>